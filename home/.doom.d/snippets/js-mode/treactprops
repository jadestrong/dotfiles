# -*- mode: snippet -*-
# name: treactprops
# uuid: treactprops
# key: treactprops
# condition: t
# --

import React from 'react';

interface $1Props {
    ${2:Param}: ${3:ParamType};
}

const ${1:name}: React.FC<$1Props> = ({ $2 }) => {
    return (
        <>$0</>
    );
};

export default $1;